type Query {
  tasks: [Task!]!
}

type Mutation {
  createTask(title: String!): Task!
}

type Task {
  id: ID!
  title: String!
  completed: Boolean!
}

# type Project {
#   name: String!
#   tasks: [Task!]!
# }
