{
  "name": "upnext",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.0"
  },
  "workspaces": ["packages/*"],
  "scripts": {
    "start": "lerna run start --stream",
    "generate": "lerna run generate --stream"
  }
}